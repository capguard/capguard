# Core dependencies (always required)
pydantic>=2.0.0

# LLM Classifier (optional but recommended)
openai>=1.0.0

# LangChain Integration (optional)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
